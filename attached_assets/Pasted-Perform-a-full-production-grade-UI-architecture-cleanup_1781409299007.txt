Perform a full production-grade UI architecture cleanup for the entire application to ensure stable mobile + desktop behavior with ZERO layout overlap issues.

GOAL:
Convert current layout into a production-safe, scalable, mobile-first architecture where ALL pages behave consistently and no UI breaks on any screen size.

────────────────────────────

1. CENTRAL LAYOUT ARCHITECTURE (CRITICAL)
- Introduce or enforce a single AppLayout system
- All pages must use shared layout structure
- Move ALL of the following into layout:
  - Bottom navigation spacing
  - Header spacing
  - Safe-area padding handling
- Remove page-level manual padding hacks (pb-16, pb-24, etc.)

────────────────────────────

2. MOBILE SAFE AREA SYSTEM
- Add proper mobile safe-area handling:
  padding-bottom: env(safe-area-inset-bottom)
- Ensure iOS Safari bottom bar does not overlap UI
- Ensure no content is hidden behind bottom navigation

────────────────────────────

3. RESPONSIVE CONSISTENCY RULE
- Every page must follow same layout rules:
  - No page-specific spacing fixes
  - No hidden elements on mobile
  - No sm:hidden that removes important features
- Mobile and desktop must have FEATURE PARITY

────────────────────────────

4. Z-INDEX + OVERLAY SYSTEM FIX
- Standardize layering:
  - Modals: z-50
  - Dropdowns: z-40 to z-50
  - Backdrops: z-30
- Ensure nothing appears behind cards or navbar
- Fix language selector + notification dropdown layering permanently

────────────────────────────

5. COMPONENT BEHAVIOR FIX
- Replace scale hover animations that cause layout shift
- Use translateY instead of scale where needed
- Ensure no component creates stacking context bugs

────────────────────────────

6. CHAT + PAGE STRUCTURE FIX
- Chat layout must never be covered by bottom nav
- Ensure scroll areas are properly contained
- Fix all h-full layouts that break on mobile

────────────────────────────

7. FINAL GOAL
- App must behave like a real production mobile application
- No overlapping UI anywhere
- No hidden buttons or features
- No layout shifts or broken spacing
- Fully stable on:
  - Mobile Safari
  - Mobile Chrome
  - Desktop Chrome

────────────────────────────

EXPECTED RESULT:
A clean, scalable, production-ready frontend architecture that will not break when new pages or features are added.