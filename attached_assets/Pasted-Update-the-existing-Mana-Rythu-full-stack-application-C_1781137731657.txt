Update the existing “Mana Rythu” full-stack application.

CRITICAL GOAL:
Fix missing AI features, ensure farmer posting system works, and remove all fake/demo data completely.

---

🚨 CORE RULES (MUST FOLLOW):
- DO NOT ignore AI feature implementation
- DO NOT use only UI placeholders for AI
- AI suggestions MUST appear in Add Crop form
- AI must NOT auto-save data (only suggest)
- NO demo/seed/fake data anywhere

---

🧠 1. AI SUGGESTION SYSTEM (MANDATORY - MUST IMPLEMENT)

In Add Crop form, implement AI helper panel:

### AI FEATURES:

A) Crop Name Suggestion:
- Based on typed input OR uploaded image placeholder logic
- Example:
  Input: "tom"
  AI Suggestion:
    - Tomato
    - Cherry Tomato

B) Fair Price Suggestion:
- Based on crop name
- Return PRICE RANGE ONLY

Example:
Tomato →
₹25 – ₹32 per kg

C) Market Trend Indicator:
- Rising 📈
- Stable ➖
- Falling 📉

---

IMPORTANT:
- AI is ONLY a suggestion system
- User must manually confirm final values
- AI must NOT auto-create listings

---

📷 2. ADD CROP FORM (FIX + CONNECT AI)

Add Crop form must include:

- Crop Image upload
- Crop Name input
- Quantity
- Price range (minPrice, maxPrice)

👉 AI panel must appear beside form:
- “Suggest Crop Name”
- “Suggest Fair Price”

---

🔥 3. MARKETPLACE FLOW (REAL DATA ONLY)

- Marketplace must fetch from database:
  GET /api/listings

- Farmers create listings using:
  POST /api/listings

- ONLY user-created listings should appear

IF NO DATA:
- Show: "No crops listed yet"

NO STATIC DATA ALLOWED.

---

🗄 4. BACKEND INTEGRATION

Ensure AI logic is handled in backend or service:

Example endpoints:
- POST /api/ai/suggest-crop
- POST /api/ai/suggest-price

OR simple internal function if no AI API is used.

---

🧾 5. DATA STRUCTURE UPDATE

CropListing must include:

- cropName
- image
- quantity
- minPrice
- maxPrice
- aiSuggested (boolean optional)
- location
- createdAt

---

🎨 6. UI REQUIREMENTS

- AI suggestion should appear as side panel or small cards
- Show "Apply suggestion" buttons
- Clean Uber/Amazon style UI
- No clutter

---

⚡ 7. SYSTEM BEHAVIOR RULES

- AI suggestion failure must NOT break app
- If AI not available, show fallback suggestions
- App must always remain functional

---

🚀 FINAL OUTPUT:

- Working Add Crop form with AI suggestions
- Marketplace connected to real user data
- No fake/demo data anywhere
- Clean production-level UI
- Fully functional MVP