Build a fully working full-stack web application called “Mana Rythu” with a PREMIUM MODERN UI like Uber, Amazon, Myntra, and Walmart dashboards.

GOAL:
A highly professional agriculture platform for farmers and buyers with world-class UI/UX design and stable backend.

TECH STACK:
- Frontend: React (Vite) + Tailwind CSS + clean component structure
- Backend: Node.js + Express
- Database: MongoDB (or fallback mock data if DB fails)

---

🔥 UI/UX DESIGN SYSTEM (VERY IMPORTANT):

Design must follow modern SaaS + e-commerce style like:
- Uber (clean cards + simplicity)
- Amazon (structured grid + product listing style)
- Myntra (visually appealing cards)
- Walmart (organized dashboard layout)

UI PRINCIPLES:
- Minimal but premium look
- White space heavy design (no clutter)
- Consistent spacing system (8px grid)
- Soft shadows (not heavy)
- Rounded cards (medium radius)
- Modern typography (Inter / system font)
- Green + white + neutral gray theme
- Smooth hover effects (subtle only)
- Mobile-first responsive design

---

📱 LAYOUT STRUCTURE:

1. GLOBAL LAYOUT
- Left sidebar (desktop)
- Bottom navigation (mobile)
- Top header with search bar
- Notification + profile icon

---

2. HOME DASHBOARD (LIKE AMAZON HOME)
- Hero summary cards:
  - Weather card 🌤
  - Mandi prices 💰
  - Market demand 📊
- Quick action buttons (Add Crop, Sell Crop)
- “Recommended crops” section (card grid)
- Clean product-like layout (Amazon style grid)

---

3. FARMER DASHBOARD (UBER STYLE SIMPLICITY)
- Large clean cards only
- Crop status cards
- “Active crops” section
- One primary CTA button: “Add Crop”

---

4. MARKETPLACE PAGE (MYNTRA STYLE GRID)
- Product-style crop cards (like shopping items)
- Each card shows:
  - Crop image placeholder
  - Name
  - Price
  - Quantity
  - Location
- Filter sidebar (like Amazon)
- Search bar at top

---

5. BUYER EXPERIENCE (UBER SIMPLE FLOW)
- Browse → Select → Contact Farmer
- No complexity
- Clean step-by-step UI

---

6. ADMIN PANEL (WALMART DASHBOARD STYLE)
- Table-based layout
- Stats cards on top
- User verification toggles
- Simple analytics cards

---

---

🧠 FEATURES:

- Farmer crop CRUD
- Marketplace listing system
- Buyer-farmer interaction
- Rating system (1–5 stars)
- Verified badge system
- Search + filter system

---

🗄 DATABASE MODELS:

User:
- id
- name
- role (farmer/buyer/admin)
- verified
- rating

Crop:
- id
- farmerId
- cropName
- sowDate
- harvestDate

Listing:
- id
- farmerId
- cropName
- price
- quantity
- location

---

🔌 API ENDPOINTS:

Auth:
POST /api/register
POST /api/login

Crops:
GET /api/crops
POST /api/crops

Marketplace:
GET /api/listings
POST /api/listings

Admin:
GET /api/users
PATCH /api/verify/:id

---

⚡ PERFORMANCE + QUALITY RULES:
- Must be fully working without errors
- Use fallback mock data if backend fails
- No broken imports
- Clean reusable components:
  - Card
  - Button
  - Table
  - Badge
- Optimized folder structure
- Fast loading UI
- No cluttered UI

---

🚀 FINAL OUTPUT:
- Fully working production-ready MVP
- Premium UI like Uber/Amazon/Myntra
- Separate frontend and backend folders
- Easy run with npm run dev