Upgrade the entire Mana Rythu frontend UI to a modern production-grade SaaS design.

GOAL:
Make the app look like a real startup product (Swiggy / Zerodha / Uber level UI quality) with smooth animations and consistent design system.

DO NOT break backend logic. Only improve UI/UX.

-----------------------------------------
1. DESIGN SYSTEM (IMPORTANT)
-----------------------------------------
- Use consistent spacing (8px grid system)
- Add modern card design with soft shadows
- Rounded corners (12px–16px)
- Use subtle borders instead of heavy lines
- Improve typography (clear hierarchy: title, subtitle, body)
- Use green as primary brand color (agriculture theme)
- Add light gradient backgrounds where needed

-----------------------------------------
2. ANIMATIONS (VERY IMPORTANT)
-----------------------------------------
Use Framer Motion style animations:

- Page load fade-in (opacity + y transition)
- Card hover:
  - slight scale (1.02)
  - shadow increase
- Button click:
  - scale down (0.97) + bounce back
- Sidebar transitions smooth slide
- Modals: fade + scale animation
- List items: stagger animation

Keep animations subtle (not flashy).

-----------------------------------------
3. DASHBOARD IMPROVEMENTS
-----------------------------------------
- Make Farmer Dashboard look like SaaS analytics panel
- Add stat cards with icons + gradients:
  - Active listings
  - Crop status
  - Earnings estimate
- Improve spacing and alignment
- Add empty states with illustrations/text

-----------------------------------------
4. MARKETPLACE UI IMPROVEMENTS
-----------------------------------------
- Listing cards must look premium:
  - image header
  - price badge
  - trend badge (up/down/stable)
  - trust score badge
- Add hover effect with smooth lift
- Improve "Place Order" section UI

-----------------------------------------
5. CHAT UI (IMPORTANT)
-----------------------------------------
- Make chat look like WhatsApp / iMessage style
- Message bubbles:
  - mine = green bubble
  - other = white/gray bubble
- Add smooth message appear animation
- Add hover actions:
  - delete icon (Trash)
- Add scroll-to-bottom animation on new message

-----------------------------------------
6. LOGISTICS WIDGET
-----------------------------------------
- Convert into modern collapsible card
- Add gradient header
- Animate expand/collapse smoothly
- Show result cards with icons + spacing improvements

-----------------------------------------
7. LOADING STATES
-----------------------------------------
Replace all boring loaders with:
- skeleton loaders for cards
- shimmer effect
- smooth transitions instead of blank screens

-----------------------------------------
8. EMPTY STATES (VERY IMPORTANT)
-----------------------------------------
Replace all "No data" screens with:
- icon + friendly message
- CTA button (example: "Add Crop", "Post Listing")

-----------------------------------------
9. MOBILE FIRST FIX
-----------------------------------------
- Ensure all pages are fully responsive
- Fix sidebar collapse on mobile
- Buttons must be full width on mobile
- No horizontal scrolling anywhere

-----------------------------------------
FINAL OUTPUT REQUIREMENT:
After changes:
- App must feel like a real production startup
- UI should be consistent everywhere
- No broken layouts
- No raw/basic HTML look

Do a full UI/UX redesign without breaking functionality.